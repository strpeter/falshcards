{% extends 'FalshcardsBundle::base.html.twig' %}

{% block content %}
<h1>Karte ändern</h1>
<form action="{{ path('save_changecard_form', {'id_card': card.idcard}) }}" method="post">
	<p>Fragestellung:</p> 
	<textarea name="question" rows="10" cols="50">{{ card.question }}</textarea><br>
	<p>Antwort:</p>
	<textarea name="answer" rows="20" cols="50">{{ card.answer }}</textarea><br>
	<p>Ordner:</p>
	<select name="folder">
		{% for folder in folders %}
		<option {% if card.ffolder.idfolder == folder.idfolder %}selected{% endif %} value="{{ folder.idfolder }}">
			{{ folder.name }}
		</option>
		{% endfor %}
	</select>
	<input type="Submit">
</form>
{% endblock %}
